<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.igalaxy.boot.mapper.sys.SysRoleResourceMapper">

    <update id="delete" parameterType="SysRoleResource">
        update tb_sys_role_resource
        set updateTime = now(),
            updateUser = #{updateUser},
            disabled = 1
        where id = #{id}
    </update>

    <insert id="save" parameterType="SysRoleResource">
        insert into tb_sys_role_resource
        (roleId, resourceId, createTime, createUser, updateTime, updateUser, disabled)
        values
        (#{roleId}, #{resourceId}, now(), #{createUser}, now(), #{updateUser}, 0)
        <selectKey resultType="long" keyProperty="id">
            select LAST_INSERT_ID() as id
        </selectKey>
    </insert>

    <select id="queryById" parameterType="long" resultType="SysRoleResource">
        SELECT
        res.`id` resourceId,
        rol.`roleId`,
        res.`name` resourceName,
        rol.`id`,
        res.`parentId`,
        role.name roleName
        FROM
        tb_sys_resource res
        LEFT JOIN tb_sys_role_resource rol
        ON res.`id` = rol.`resourceId`
        AND rol.`disabled` = 0
        left join tb_sys_role role
            on role.id = rol.roleId
        WHERE rol.id = #{id}
    </select>

    <update id="deleteByRoleIdResourceId" parameterType="map">
        update tb_sys_role_resource
        set updateTime = now(),
        updateUser = #{updateUser},
        disabled = 1
        where roleId = #{roleId} AND resourceId = #{resourceId} AND disabled = 0
    </update>

</mapper>